---
verify-request:
  instructions: "Has the user provided which numbers to calculate? If so go to calculate, otherwise ask."
  links: [ "ask-for-calculation", "calculate" ]

ask-for-calculation:
  instructions: "What calculation would you like to complete?"

calculate:
  instructions: "Calculate the provided equation."
  tools: [ "calculator" ]