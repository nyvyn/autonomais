{
  "name": "autonomais",
  "version": "0.0.1",
  "description": "An approachable way to define multiple, collaborative AI agents",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "tsc",
    "run example": "ts-node ./src/cli.ts ./examples/weather.yaml",
    "test": "jest",
    "update scripts": "npx npm-check-updates -u"
  },
  "dependencies": {
    "@langchain/core": "^0.1.55",
    "@langchain/langgraph": "0.0.12",
    "@langchain/openai": "^0.0.26",
    "yaml": "^2.4.1",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2"
  }
}
